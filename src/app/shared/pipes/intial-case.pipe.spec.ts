import { IntialCasePipe } from './intial-case.pipe';

describe('IntialCasePipe', () => {
  it('create an instance', () => {
    const pipe = new IntialCasePipe();
    expect(pipe).toBeTruthy();
  });
});
